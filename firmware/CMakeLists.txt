# ESP32 Tamagotchi - Top-level CMakeLists.txt
cmake_minimum_required(VERSION 3.16)

# Include ESP-IDF project configuration
include($ENV{IDF_PATH}/tools/cmake/project.cmake)

project(esp32-tamagotchi)
